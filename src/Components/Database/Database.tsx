import React from "react";

const Database=() => {
    return <div className="database">
        <h1> Database </h1>

        {/* tabs navigation */}
        {/* tabs inner content */}
    </div>;
}

export default Database;